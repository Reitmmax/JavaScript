var person = { // defining the variable
    first: "Bob", 
    last: "Smith", // defining the methods
    age: 23,
};
    
var firstName = person.first; // using objects to turn into variables
var lastName = person.last;
var age = person.age;

document.write(firstName); // writing out the objects
document.write(" " + lastName);
document.write(" " + age);
    
    